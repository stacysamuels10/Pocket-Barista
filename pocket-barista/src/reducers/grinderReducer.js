const initialState = {
  grinder: {
    name: "",
    brand: "",
  },
  grinderPantry: [],
};

const grinderReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default grinderReducer;
